# Wallach Assessoria Jurídica — site

Site institucional (Next.js) em [`web/`](./web/).

## Desenvolvimento

```bash
cd web && npm install && npm run dev
```

## Build

```bash
cd web && npm run build
```
